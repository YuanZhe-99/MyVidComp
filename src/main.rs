// AI-FUNC-SUMMARY: Runs the command-line workflow; returns process result through normal CLI control flow; side effects: reads config, runs ffmpeg/ffprobe, writes logs and files.
fn main() {
    myvidcomp_core::main_entry();
}
